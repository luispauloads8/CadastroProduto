import { EmailMaskDirective } from './email-mask.directive';

describe('EmailMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailMaskDirective();
    expect(directive).toBeTruthy();
  });
});
